name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write
  checks: write
  pull-requests: write
  id-token: write
  security-events: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      ##############################################################
      # 🧱 Project setup & build
      ##############################################################

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Run tests and generate coverage reports
        run: ./gradlew clean build test jacocoTestReport jacocoRootReport --no-daemon

      ##############################################################
      # 📦 Upload test and coverage reports as artifacts
      ##############################################################

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: build/reports/tests/test/

      - name: Upload Jacoco coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: build/reports/jacoco/

      ##############################################################
      # 📊 SonarCloud static code analysis
      ##############################################################

      - name: SonarCloud Analysis
        uses: SonarSource/sonarcloud-github-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=marcoslozina_java-springboot-reactive-hexagonal-multimodule-template
            -Dsonar.organization=marcoslozina
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.coverage.jacoco.xmlReportPaths=build/reports/jacoco/jacocoRootReport/jacocoRootReport.xml

      ##############################################################
      # 📥 Submit dependency graph to GitHub
      ##############################################################

      - name: Submit dependency graph
        uses: gradle/actions/dependency-submission@v5

      ##############################################################
      # 📊 Generate coverage and vulnerability JSONs
      ##############################################################

      - name: Install required tools
        run: sudo apt-get update && sudo apt-get install -y libxml2-utils jq

      - name: Generate coverage.json
        run: |
          mkdir -p .badge-data
          coverage=$(xmllint --xpath "string(//report/counter[@type='INSTRUCTION']/@covered)" build/reports/jacoco/jacocoRootReport/jacocoRootReport.xml || echo "0")
          missed=$(xmllint --xpath "string(//report/counter[@type='INSTRUCTION']/@missed)" build/reports/jacoco/jacocoRootReport/jacocoRootReport.xml || echo "0")
          total=$((coverage + missed))

          if [ "$total" -eq 0 ]; then
            percent="0.0"
          else
            percent=$(awk "BEGIN {printf \"%.1f\", ($coverage/$total)*100}")
          fi

          if (( $(echo "$percent >= 80" | bc -l) )); then
            color="brightgreen"
          elif (( $(echo "$percent >= 50" | bc -l) )); then
            color="yellow"
          else
            color="red"
          fi

          jq -n \
            --arg percent "$percent%" \
            --arg color "$color" \
            '{schemaVersion:1,label:"coverage",message:$percent,color:$color}' \
            > .badge-data/coverage.json

      - name: Generate security.json (from SonarCloud)
        run: |
          mkdir -p .badge-data

          vulnerabilities=$(curl -s -u "${{ secrets.SONAR_TOKEN }}:" \
            "https://sonarcloud.io/api/measures/component?component=marcoslozina_template-service&metricKeys=vulnerabilities" \
            | jq -r '.component.measures[0].value // "0"')

          if (( vulnerabilities == 0 )); then
            color="brightgreen"
          elif (( vulnerabilities < 10 )); then
            color="yellow"
          else
            color="red"
          fi

          jq -n \
            --arg count "$vulnerabilities" \
            --arg color "$color" \
            '{schemaVersion:1,label:"Vulnerabilities",message:$count,color:$color}' \
            > .badge-data/security.json

      - name: Show updated security badge
        run: |
          echo "📋 Contents of security.json:"
          cat .badge-data/security.json
          echo "--- Git status ---"
          git status

      ##############################################################
      # 🚀 Deploy badge data to GitHub Pages (gh-pages branch)
      ##############################################################

      - name: Commit and push badge files to gh-pages
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

          git fetch origin gh-pages || true
          git worktree add gh-pages gh-pages || git checkout --orphan gh-pages

          cp .badge-data/*.json gh-pages/
          cd gh-pages
          git add *.json
          git commit -m "📊 Update badges" || echo "No changes to commit"
          git push origin HEAD:gh-pages

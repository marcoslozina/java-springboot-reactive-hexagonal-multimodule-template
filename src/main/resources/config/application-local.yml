server:
  port: 8081

spring:
  application:
    name: template-service
  r2dbc:
    url: r2dbc:h2:mem:///devdb
    username: dev_user
    password: dev_pass
  sql:
    init:
      mode: always

logging:
  level:
    root: DEBUG